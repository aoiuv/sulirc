grep: 2: No such file or directory
brace-expansion.bash:now=`date +%T`
brace-expansion.bash:now_=$(date +%T)
catfile:     the standard output.
catfile:             Only a count of selected lines is written to standard output.
catfile:             specify multiple patterns, or when a pattern begins with a dash
catfile:             to standard output.  Pathnames are listed once per file searched.
catfile:             If the standard input is searched, the string ``(standard
catfile:             standard output.  ggrreepp will only search a file until a match has
catfile:             names are listed once per file searched.  If the standard input
catfile:             is searched, the string ``(standard input)'' is written.
catfile:             Silent mode.  Nonexistent and unreadable files are ignored (i.e.
catfile:             buffered when standard output is a terminal and block buffered
catfile:     If no file arguments are specified, the standard input is used.
catfile:           $ egrep '19|20|25' calendar
catfile:     Peruses the file `calendar' looking for either 19, 20, or 25.
environment_variables: $PS2     The secondary prompt string.
kill-port.js:    exec(`lsof -i tcp:${port}`, (err, data) => {
kill-port.js:      const result = data.match(regexNodePort);
kill-port.js:    exec(`kill -9 ${PID}`, (err, data) => {
kill-port.js:      resolve(data);
readme.md:Update macOS bash version to > v3
readme.md:https://apple.stackexchange.com/questions/193411/update-bash-to-version-4-0-on-osx
